'use strict'

const {
  diContainer,
  fastifyAwilixPlugin,
  diContainerProxy,
  diContainerClassic
} = require('./lib/fastifyAwilixPlugin')

module.exports = {
  diContainer,
  diContainerClassic,
  diContainerProxy,
  fastifyAwilixPlugin
}
