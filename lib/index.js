'use strict'

const {
  diContainer,
  fastifyAwilixPlugin,
  diContainerProxy,
  diContainerClassic
} = require('./fastifyAwilixPlugin')

module.exports = {
  diContainer,
  diContainerClassic,
  diContainerProxy,
  fastifyAwilixPlugin
}
